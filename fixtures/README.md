# Fixtures

For the protocol inputs: a published ballot box, the collected certificates, the announced
result. None of them exist yet, docs/format.md still has to define the format and votura
publishes nothing to fill it with.

Nothing reads this folder yet.

Not the same as `public/api/verificationReports/`. Those are finished reports that the interface
fetches and displays. This folder is for the raw input a check would run on.

Never commit real election data here.
