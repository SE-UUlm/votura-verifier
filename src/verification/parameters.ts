/**
 * Route parameter names, kept in one place so that the router, the views and the links cannot
 * drift apart over a typo.
 */
export const parameter = {
  verificationReportId: 'verificationReportId',
} as const;
